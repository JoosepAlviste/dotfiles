# Settings

# Do not show these files by default
set hidden_filter ^\.|~$|\.(?:pyc|log)$|^lost\+found$|^__(py)?cache__$

# Show preview images (requires w3m)
set preview_images true


# Commands

map mkd console mkdir%space
map D console delete

map <C-f> fzf_select
map <C-g> fzf_locate

# Tar the selected files
map Z shell tar -cvzf %f.tar.gz %f
map X shell extract %f


# Movement

map gw cd ~/Devel/work

map gd cd ~/Downloads

map gp cd ~/Pictures

map gs cd /media/samsung/Files


# Files

map cfd shell vim ~/dotfiles

