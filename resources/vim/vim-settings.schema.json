{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://github.com/JoosepAlviste/dotfiles/blob/master/resources/vim/vim-settings.schema.json",
  "type": "object",
  "title": "My Vim settings schema",
  "description": "This schema specifies the custom Vim configuration thingy that my Vim set up can read.",
  "default": {
    "flow.enabled": false,
    "tsserver.enabled": true
  },
  "examples": [
    {
      "flow.enabled": true,
      "tsserver.enabled": false
    }
  ],
  "required": [],
  "additionalProperties": true,
  "properties": {
    "flow.enabled": {
      "$id": "#/properties/flow.enabled",
      "type": "boolean",
      "title": "Enable Flow LSP",
      "description": "Flow should only be enabled in Flow projects.",
      "default": false,
      "examples": [
        true,
        false
      ]
    },
    "tsserver.enabled": {
      "$id": "#/properties/tsserver.enabled",
      "type": "boolean",
      "title": "Enable TypeScript support",
      "description": "TypeScript support should usually be enabled. It also works in JavaScript files!",
      "default": true,
      "examples": [
        true,
        false
      ]
    }
  }
}
